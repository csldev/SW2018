<!-- index.wxml -->
<view class="container">
  <view class="btns" wx:for="{{items}}" wx:key="id">
    <button class="btn" catch:tap="onButtonTapped" data-id="{{item.id}}">{{item.name}}</button>
  </view>
</view>